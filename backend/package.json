{
  "name": "kb-flow-realtime-presence",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal realtime presence backend for cross-device admin telemetry.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
